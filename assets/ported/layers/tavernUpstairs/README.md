# tavernUpstairs

These assets were extracted from the original layered Procreate document without modifying it.

- Canvas: 1440 x 3120 px
- Source layers: 44
- Exported transparent PNGs: 43
- Stable asset IDs: `ELD-ENC-TU-001` onward, following the source's top-to-bottom layer order
- Exact original placement is stored in `AssetManifest.json` as `pixelRect` and `anchorNormalized`

Use the ID, rather than the filename, for future allocation and references. Renaming a display label does not change its ID.
